# 자율주행 작업 일지

> 오토카 프로젝트에서 "자율주행 담당"(라이다+YOLO+OpenCV 전부)으로 진행한 내용을 정리한 문서입니다.
> 초보자도 이해할 수 있게 최대한 쉽고 자세하게 적었습니다.
> 앞으로 작업이 진행될 때마다 이 문서를 계속 업데이트합니다.

## 센서/기술 역할 분담

전부 내 담당.

| | 역할 |
|---|---|
| **라이다** | 360도 전방위, 정확한 거리 측정 → 정지/회피 여부 결정(안전 메인) |
| **YOLO** | 정면 카메라로 물체 종류 판단(사람/자동차 등) → TTS 안내에 사용(보조) |
| **OpenCV** | 트랙 선 색상 검출 → 두 선 사이 중앙 유지(라인 트레이싱) |

## ✅ 진행 상황 요약

팀 프로젝트에서 **자율주행(라이다+YOLO+OpenCV) 전체가 담당 범위**이며, 아래 내용까지 진행되었습니다.

- 오토카(Jetson)에서 YOLOv3-tiny 실시간 객체 탐지 성공 (신뢰도 최대 0.98)
- TensorRT 변환으로 **FPS 6.0 → 30.1**, 5배 성능 개선
- 이 결과(박스 위치, 클래스, 신뢰도)를 다른 팀원(조향/속도 제어 담당)에게 넘겨서 자율주행 로직에 연결하면 됨
- 참고용으로 "탐지 결과를 실제 조향/속도 명령으로 바꾸는" 예시 코드(`jetson_obstacle_avoidance.py`)까지 만들어서 **"정지 후 생각"(stop-then-think) 방식**으로 설계함 — 다만 모터의 실제 반응 속도·정밀 튜닝은 담당 범위 밖이라 팀의 하드웨어 제어 담당자 몫으로 넘김
- 카메라는 화각(FOV)이 제한적이라 정면 좁은 범위만 보이므로, 360도 전체를 보는 **라이다와 함께 쓰는 것을 권장** (`sensor_fusion.py`에 설계 방식 정리해둠, 실측/실기 테스트는 라이다 담당자 몫)

---

## 0. 이 프로젝트에서 YOLO를 왜 쓰는가

- **YOLO(You Only Look Once)**: 카메라로 찍은 이미지 안에서 "무엇이(사람/자동차 등)" "어디에" 있는지 실시간으로 찾아주는 AI 모델(객체 탐지 모델)
- 우리 오토카에서 쓰는 이유:
  1. **장애물 감지**: 앞에 사람/물체가 있으면 인식해서 정지 또는 회피
  2. **음성 알림**: 장애물이 있으면 TTS(음성)로 알려주기
- on-device AI 프로젝트라, Jetson 보드 위에서 직접 돌아가야 하고, 성능이 낮은 장비라 **가벼운(경량) 모델**을 써야 함

---

## 1. 개발 환경 구조 이해하기

| 환경 | 역할 |
|---|---|
| **WSL(우분투, 이 컴퓨터/노트북)** | 코드를 미리 짜고, 웹캠으로 기본 테스트하는 "연습장" |
| **오토카(Jetson Nano)** | 실제로 로봇에 들어가서 돌아가는 "진짜 실행 환경" |

**중요한 차이점:**
- WSL: Python 3.10, 최신 라이브러리(ultralytics YOLOv8) 사용 가능
- Jetson: **Python 3.6.9**, **OpenCV 4.3.0** (2020년에 나온 구버전, JetPack 4.3) → 최신 라이브러리 대부분 설치가 안 됨

---

## 2. WSL에서 한 것 (1단계: 기본 개념 익히기 + 연습)

### 2-1. 가상환경 만들기
- 위치: `Autocar 최종 프로그램/.venv`
- 왜 필요한가: 프로젝트마다 필요한 파이썬 패키지 버전이 다를 수 있어서, 서로 안 꼬이게 독립된 공간을 만드는 것

### 2-2. YOLOv8n(nano)으로 정지 이미지 탐지 테스트
- 파일: `test_yolo.py`
- `ultralytics` 라이브러리의 `yolov8n.pt`(가장 가벼운 버전) 사용
- 결과: 버스 사진에서 사람 3명, 버스 1대 정확히 탐지 성공

### 2-3. 웹캠 실시간 탐지
- 파일: `webcam_yolo.py`
- USB 웹캠을 `usbipd-win`으로 WSL에 연결해서 실시간 탐지 테스트
- **겪은 문제와 해결:**
  - 화면이 초록색으로만 나옴 → 카메라 영상 포맷(YUYV)을 OpenCV가 잘못 해석하는 문제 → `MJPG` 포맷으로 강제 지정해서 해결
  - Qt 창이 안 뜸 → `QT_QPA_PLATFORM=xcb` 환경변수로 해결
  - TTS 말하는 동안 화면이 멈춤 → TTS를 별도 스레드(`threading`)에서 실행하도록 수정

### 2-4. TTS(음성 알림) 연결
- 파일: `tts_util.py` (공용 모듈, 여러 파일에서 재사용)
- `espeak-ng`(오프라인 음성 합성) + `paplay`(WSL 재생) 조합
- 사람이 감지되면 "전방에 장애물이 있습니다" 음성 안내
- 3초 쿨다운을 둬서 너무 자주 말하지 않도록 함

### 2-5. 라이다 센서 퓨전 (설계만, WSL 테스트 불가)
- 파일: `sensor_fusion.py`
- 라이다 드라이버(`pop/LiDAR/_rplidar.so`)가 **ARM(Jetson) 전용**이라 WSL(x86_64)에서는 아예 실행 불가
- YOLO가 찾은 물체의 화면 속 위치(픽셀) → 각도로 변환 → 그 각도의 라이다 거리값과 매칭하는 로직만 미리 작성해둠
- 실측 필요한 값: 카메라 화각(FOV), 라이다 0도 기준 방향

### 2-6. 장애물 회피 로직 (설계만, WSL 테스트 불가)
- 파일: `obstacle_avoidance.py`
- `pop.Pilot.AutoCar()`(실제 오토카 제어 클래스) 기반으로 작성
- 로직: 화면에서 가장 큰(=가장 가까운) 물체를 찾아서
  - 아주 가까우면 → 정지 + 음성 경고
  - 어느 정도 가까우면 → 물체 반대쪽으로 핸들 꺾어서 회피
  - 멀면 → 그냥 직진

### 2-7. 카메라는 정면 고정, 나머지 방향은 라이다 담당
- `pop/CAN.py`의 `Car` 클래스에 `camPan()`/`camTilt()`(카메라 팬/틸트 서보 제어) 함수가 존재하긴 하지만, YOLO 코드에서는 **이 기능을 쓰지 않고 카메라가 정면 고정이라고 가정**하고 작성함
- 이유: 카메라가 움직이면 "화면 속 좌우 위치 → 실제 방향" 계산 로직이 팬 각도까지 고려해야 해서 훨씬 복잡해짐
- 역할 분담: **카메라(YOLO)는 정면만 담당, 측면/후방 등 나머지 방향은 라이다가 담당**하는 구조로 가는 게 가장 현실적
- **카메라와 라이다는 서로 상호보완 관계:**
  - 카메라(YOLO): 정면만 보지만 "이게 사람인지 자동차인지" **종류**를 구분할 수 있음 (라이다는 종류를 모름, "뭔가 있다"만 앎)
  - 라이다: 360도 전체를 보고 **정확한 거리**를 알지만, "그게 뭔지"는 모름
  - 둘을 합치면(`sensor_fusion.py`) → "정면 30cm 앞에 사람이 있다" 같은 완성된 정보가 나옴

### 2-8. 최종 역할 분담 방향 정리
여러 시행착오(특히 구버전 Jetson과 최신 도구 간 호환성 문제) 끝에, 아래처럼 역할을 나누는 것으로 방향을 정함:

| 기능 | 담당 | 이유 |
|---|---|---|
| **라인 트레이싱**(트랙 두 선 사이 가운데 유지) | **OpenCV 색상 검출** | 선은 색이 뚜렷해서 딥러닝 없이도 빠르고 정확함 |
| **장애물 회피(안전)** | **라이다(메인)** | 360도 전방위, 정확한 거리, Jetson 버전 호환성 문제 없음 |
| **장애물 종류 인식** | **YOLO(보조)** | "뭔가 있다"가 아니라 "사람이다/자동차다"처럼 종류를 구분해서 TTS 안내에 활용 |

즉 라이다가 "멈출지 말지"를 결정하는 **안전 담당**이고, YOLO는 그 위에 "무엇을 봤는지" 설명을 얹어주는 **보조 담당**으로 구성.

### 2-9. 라인 트레이싱 코드 작성 (OpenCV 색상 검출 방식)
- 파일: `line_tracing.py`, `line_calibration.py`
- **원리**: HSV 색공간에서 흰색/노란색 범위를 필터링해서 선 픽셀만 골라내고, 화면을 좌/우로 나눠 각각 선의 위치를 찾은 뒤 그 정가운데를 트랙 중심으로 계산 → 화면 중심과의 차이만큼 조향
- `line_calibration.py`: 실제 트랙 사진 한 장을 찍어서 색상 필터가 잘 맞는지 미리 확인하는 보정용 스크립트 (`calib_original.jpg`, `calib_mask.jpg`, `calib_overlay.jpg` 생성)
- ⏳ **아직 실기 테스트 안 함** — 실제 트랙 색상에 맞게 `LOWER_WHITE`/`UPPER_WHITE`/`LOWER_YELLOW`/`UPPER_YELLOW` 값 보정 필요
- 카메라는 팬/틸트 없이 **정면 고정**이라고 가정하고 설계함 (실제로 `pop/CAN.py`에 `camPan`/`camTilt` 기능이 존재하지만 이 코드에서는 사용하지 않음)

### 2-10. 실제 트랙 현장 색상 보정 시도 (미완료, 다음에 이어서)

운동장 트랙에서 `line_calibration.py`로 여러 번 시도함. 겪은 문제와 시행착오:

| 시도 | 조건 | 인식 비율 | 문제 |
|---|---|---|---|
| 1차 | 카메라가 해를 정면으로 바라봄, HSV 흰색/노란색 고정값 | 64.6% | 역광으로 화면 절반이 하얗게 날아가서(과다노출) 하늘과 선을 구분 못함 |
| 2차 | Otsu 자동 임계값으로 변경 | 65.4% | 여전히 역광 문제로 큰 차이 없음 |
| 3차 | 채도(S) 기준으로 변경, ROI를 화면 아래 25%로 좁힘 | 93.9% | ROI가 선이 없는 순수 트랙 바닥만 보고 있었음 (선은 화면 중간쯤에 위치) |
| 4차 | ROI를 화면 40%~100%로 넓힘 | 33.9~39.8% | 지평선(나무/건물 배경)까지 밝게 잡혀서 오인식 |
| 5차 | 오토카를 해를 등지게 회전, ROI를 52%~100%로 좁힘 | 26.1% | 여전히 배경 일부 포함 |
| 6차 | 카메라 각도를 손으로 아래로 내려 지평선 자체를 화면에서 제거 | **2.7%** | 역광 문제는 해결됐으나 이번엔 기준이 너무 엄격해서 선을 거의 못 잡음 |
| 7차 | 채도/명도 기준 완화 (S<70, V>170) | 64.6% | 이번엔 너무 헐렁해져서 트랙 바닥 전체가 다 잡힘 |
| 8차 | `color_sample.py`로 선/트랙의 정확한 HSV 수치 직접 측정 | - | 측정해보니 선(S=52~74)과 트랙(S=65)의 채도 차이가 거의 없어서, 지금 조명(핑크빛 도는 색감)에서는 색상만으로 깔끔하게 구분하기 어려움을 확인 |

**결론 및 다음 시도 방향:**
- 역광(태양 정면)은 카메라 각도를 낮춰서(지평선 미포함) 어느 정도 해결 가능함을 확인
- 다만 이 조명 조건(맑은 날 정오 근처, 카메라 색감이 핑크빛으로 치우침) 자체가 흰 선과 빨간 트랙의 색 대비를 약하게 만들어서, HSV 고정값 튜닝만으로는 한계가 있음
- **다음에 시도해볼 것**: (1) 흐린 날 또는 저녁 무렵 등 역광이 덜한 조건에서 재시도, (2) `color_sample.py`로 정확한 좌표에서 다시 측정, (3) 색상 대신 엣지(경계선) 검출 방식도 고려, (4) 카메라 자체의 화이트밸런스 설정 확인

### 2-11. 라인 트레이싱 + 장애물 회피 통합 (핵심 설계: "목표 지점을 옆으로 잠깐 옮기기")
- 파일: `line_tracing_with_avoidance.py`
- **요구사항**: 장애물의 종류는 중요하지 않지만, **회피 후 원래 라인으로 자연스럽게 복귀하는 것이 중요함**
- **설계 아이디어**:
  - 회피할 때 "라인 추적을 끄고 blind하게 핸들을 꺾는" 방식이 아니라
  - 라인 추적 계산 로직은 **항상 똑같이 계속 돌아가고**, 장애물이 있을 때만 "목표로 삼는 중심 위치"를 옆으로 잠깐(`AVOID_OFFSET_PIXELS`만큼, `AVOID_DURATION_SEC`초 동안) 옮김
  - 회피 시간이 끝나면 목표 지점이 자동으로 화면 정중앙으로 돌아가므로, **같은 제어 로직이 자연스럽게 원래 라인 중심으로 복귀시켜줌**
- YOLO(TensorRT)는 "장애물이 왼쪽/오른쪽 어디에 있는지"만 판단하는 데 사용 (종류는 안 씀)
- ⏳ **아직 실기 테스트 안 함**

### 2-12. 사람 따라가기 (YOLO person 추적)
- 파일: `jetson_follow_person.py`
- 라인 트레이싱은 색상 보정 문제로 잠시 보류하고, 대신 색상 보정이 필요 없는 **사람 추적 기능**을 먼저 만듦
- 원리: YOLO로 "person" 클래스만 찾아서, 가장 큰(가까운) 사람을 기준으로
  - 화면 중앙에 오도록 조향
  - 박스 크기(거리 추정)로 전진/정지 결정 (멀면 전진, 적당하면 서행, 가까우면 정지)
- 사람이 안 보이면 안전하게 정지
- ⏳ **작성만 완료, 실기 테스트는 다음에 (오늘은 더위로 인해 현장 종료)**

---

## 3. Jetson(실제 오토카)에서 한 것 (2단계: 실기 테스트)

### 3-1. 접속 방식
- USB로 연결 후 SSH로 오토카(`soda@192.168.55.1`)에 터미널 접속

### 3-2. 버그 발견 및 수정: `pop/Pilot.py`
- `from pop import Pilot` 시도 시 `NameError: name 'np' is not defined` 에러
- 원인: 파일 맨 위에 `import numpy as np`가 빠져 있었음 (라이브러리 자체의 버그)
- 해결: `import subprocess as sp` 다음 줄에 `import numpy as np` 추가

### 3-3. YOLOv4-tiny 시도 → 실패 → YOLOv3-tiny로 전환
- 처음엔 `pop/model/yolov4-tiny/`에 있던 `yolov4-tiny.weights`를 OpenCV DNN으로 돌리려 함
- `.cfg`(모델 구조 파일)가 없어서 다운로드 받아 추가
- **문제**: 탐지된 물체 수가 항상 0, 신뢰도도 항상 0
- **원인**: OpenCV 4.3.0은 YOLOv4 계열의 최신 구조(CSP 그룹 연산)를 지원하지 않음 (OpenCV 4.4부터 정식 지원)
- **해결**: 더 단순한 구조라 OpenCV 4.3에서도 확실히 동작하는 **YOLOv3-tiny**로 교체
  - 신뢰도 0.98까지 나오며 정상 작동 확인

### 3-4. 실시간 카메라 연동 (CPU 버전)
- 파일: `jetson_yolo_camera.py`
- CSI 카메라(`Util.gstrmer`) + YOLOv3-tiny(OpenCV DNN) 연결
- 결과: **FPS 6.0** (CPU로만 연산해서 느림)

### 3-5. CUDA 백엔드 시도 → 실패
- `cv2.dnn`에 CUDA 백엔드를 지정했지만 "DNN module was not built with CUDA backend" 에러
- 원인: 이 Jetson에 설치된 OpenCV는 DNN 모듈이 CUDA 지원 없이 빌드되어 있음 (별도로 다시 컴파일해야 하는 부분)

### 3-6. TensorRT 변환 (최종 성공!)
Jetson 전용 고속 실행 엔진(TensorRT)으로 변환하는 작업. 여러 단계의 버전/설정 문제를 하나씩 해결함.

**전체 흐름:**
```
YOLOv3-tiny(다크넷 형식) → ONNX(중간 변환 파일) → TensorRT 엔진(.trt)
```

**겪은 문제와 해결 순서:**

| # | 문제 | 원인 | 해결 |
|---|---|---|---|
| 1 | `onnx==1.4.1` 설치 실패 (`Unknown generator option: dllexport_decl`) | onnx 버전과 protobuf 버전이 안 맞음 | `onnx==1.9.0` + 최신 protobuf로 재설치 |
| 2 | `pycuda` 설치 스크립트에서 `nvcc not found` | CUDA 툴킷 경로가 PATH에 없음 | `~/.bashrc`에 `/usr/local/cuda/bin` 경로 추가 |
| 3 | `pycuda` 설치 스크립트가 `basename` 에러로 중단 | 스크립트 내부 로직 버그 | `pip3 install pycuda`로 직접 설치 |
| 4 | `yolo_to_onnx.py` 실행 시 "file not found" | 파일 이름에 해상도(`-416`)가 안 붙어 있었음 | `yolov3-tiny-416.cfg`/`.weights`로 이름 맞춰 복사 |
| 5 | 플러그인 빌드 시 `libyolo_layer.so` 없음 | `plugins` 폴더에서 빌드(`make`)를 안 함 | `cd plugins && make` 실행 |
| 6 | `make` 실행 시 `unterminated call to function 'shell'` | Makefile 안 정규식에 `#` 문자가 있어서, make가 그 뒤를 "주석"으로 착각 | `grep -Po`(정규식) 대신 `awk`로 대체 |
| 7 | `onnx_to_tensorrt.py` 실행 시 `build_serialized_network` 없음 | 설치된 TensorRT(7.1.3)가 그 함수를 지원하는 8버전보다 낮음 | TensorRT 7 API인 `build_engine`으로 코드 수정 |

**최종 결과:**
- `yolov3-tiny-416.trt` 엔진 파일 생성 성공
- 실시간 카메라 테스트 결과: **FPS 30.1** (카메라 최대 속도까지 도달, 기존 6.0 대비 5배 향상)

---

## 4. 지금까지 만든 파일 목록

### WSL (`Autocar 최종 프로그램 이서현/`) — 2026-07-15 기준. 사람따라가기 프로젝트 실제 사용 파일은 `사람따라가기_프로젝트/` 하위 폴더로 정리함
| 파일 | 설명 | 테스트 상태 |
|---|---|---|
| `사람따라가기_프로젝트/phone_remote_control.py` | **현재 메인 프로그램.** 폰 조종(MANUAL) + YOLO 사람 추적(AUTO) + 라이다 안전 회피(항상 최우선, 별도 프로세스) + 사람 놓치면 카메라 탐색, 전부 통합 | ✅ 실기 조종 + 라이다 회피 동작 확인 완료 |
| `사람따라가기_프로젝트/launcher.py` | 폰에서 시동/종료 버튼으로 `phone_remote_control.py`를 tmux로 켜고 끄는 상시 대기 서버 (포트 5001) | ✅ 실행 확인 |
| `사람따라가기_프로젝트/lidar_reader.py` | 라이다 전용 독립 프로세스. 좌/우 근접 장애물 여부를 `/tmp/lidar_state.json`에 기록만 함 | ✅ 실행/감지 확인 |
| `사람따라가기_프로젝트/templates/remote.html` | 폰 조종 대시보드 화면 (팀 저장소 원본) | ✅ 정상 표시 |
| `webcam_yolo.py` | WSL 웹캠 실시간 탐지 + TTS (연습용) | ✅ 완료 |
| `tts_util.py` | 공용 음성 알림 모듈 | ✅ 완료 |
| `cam_debug.py` | 카메라 포맷 디버깅용 | ✅ 완료 |
| `sensor_fusion.py` | 카메라+라이다 방향/거리 계산 설계 (참고용, 실제 통합은 `phone_remote_control.py`에 반영됨) | 🗄️ 참고용 보관 |
| `jetson_obstacle_avoidance.py` | Jetson용 장애물 회피 (TensorRT, "정지 후 생각" 방식, 초기 버전) | 🗄️ 참고용 보관, 실제 사용은 `phone_remote_control.py`의 라이다 회피 로직 |
| `line_tracing.py` / `line_calibration.py` / `color_sample.py` / `line_tracing_with_avoidance.py` | 라인 트레이싱 관련 (보류 중, 시간 남으면 재시도 예정) | 🔄 보류 |

### Jetson (`~/tensorrt_demos/`, `~/pop/model/`)
| 파일/폴더 | 설명 |
|---|---|
| `~/pop/model/yolov3-tiny/` | YOLOv3-tiny 원본 가중치(cfg, weights, names) |
| `~/tensorrt_demos/` | TensorRT 변환 도구 (오픈소스 clone) |
| `~/tensorrt_demos/yolo/yolov3-tiny-416.onnx` | ONNX 변환 결과 |
| `~/tensorrt_demos/yolo/yolov3-tiny-416.trt` | **최종 TensorRT 엔진 (완성본)** |
| `~/tensorrt_demos/headless_test.py` | TensorRT 엔진 속도 테스트용 (화면 없이 콘솔 출력) |

---

## 5. 겪었던 문제와 해결법 모음 (트러블슈팅 총정리)

여기저기 흩어져 있던 문제/해결을 한 곳에 모았습니다. 나중에 비슷한 문제가 또 생기면 여기부터 찾아보면 됩니다.

### WSL(연습 환경)에서 겪은 문제

| 문제 | 증상 | 원인 | 해결 |
|---|---|---|---|
| 가상환경 생성 실패 | `ensurepip is not available` | `python3-venv` 패키지가 시스템에 없음 | `sudo apt install python3.10-venv` |
| 가상환경 이름 바꾼 후 안 됨 | `ModuleNotFoundError: No module named 'ultralytics'` | `.venv` 폴더만 `mv`로 이름 바꿨더니, 내부 `activate` 스크립트에 옛날 경로(`yolo_env`)가 그대로 남아있었음 | 가상환경을 통째로 삭제하고 새 이름으로 재생성 |
| 웹캠 화면이 초록색으로만 나옴 | 카메라는 열리는데 영상이 이상함 | usbipd로 넘어온 웹캠의 기본 영상 포맷(YUYV)을 OpenCV가 잘못 해석 | `cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc(*'MJPG'))`로 포맷 강제 지정 |
| 영상 창이 안 뜨고 에러남 | `Could not find the Qt platform plugin "wayland"` | WSLg가 기본으로 wayland를 쓰는데, OpenCV의 Qt 플러그인은 xcb만 지원 | 실행 시 `QT_QPA_PLATFORM=xcb` 환경변수 지정 |
| 웹캠 권한 없음 | `/dev/video0` 접근 불가 | 현재 계정이 `video` 그룹에 속해있지 않음 | `sudo usermod -aG video $USER` 후 재로그인 (즉시 반영하려면 `sg video -c "명령어"`) |
| 소리가 안 남 | `aplay` 실행 시 `Unknown PCM default` | WSL은 ALSA 사운드카드가 없고 PulseAudio(WSLg)만 있음 | `aplay` 대신 `paplay` 사용 (+ `pulseaudio-utils` 설치) |
| 소리 여전히 안 남 | 명령어는 성공(exit 0)하는데 소리가 안 들림 | 노트북 자체 스피커 음소거 상태였음 | 노트북 볼륨 확인 (당연하지만 은근히 놓치기 쉬움) |
| 카메라 화면과 `q` 키가 멈춤 | 사람 감지 시 화면이 뚝뚝 끊기고 `q`도 안 먹힘 | TTS 음성 재생(`subprocess.run`)이 끝날 때까지 메인 루프 전체가 멈춰버림(동기 처리) | TTS 재생을 `threading.Thread`로 분리해서 화면 루프와 동시에 돌아가게 함 |

### Jetson(실제 오토카)에서 겪은 문제

| 문제 | 증상 | 원인 | 해결 |
|---|---|---|---|
| `pop.Pilot` import 실패 | `NameError: name 'np' is not defined` | `pop/Pilot.py` 파일에 `import numpy as np`가 아예 빠져있던 라이브러리 자체 버그 | 파일 상단에 `import numpy as np` 추가 |
| YOLOv4-tiny 탐지 결과 항상 0개 | 신뢰도(confidence)가 항상 0으로 나옴 | 이 Jetson의 OpenCV(4.3.0)가 YOLOv4 계열의 최신 구조(CSP group 연산)를 지원 안 함 (OpenCV 4.4부터 지원) | 더 단순한 구조인 **YOLOv3-tiny**로 교체 |
| CUDA로 설정해도 그대로 CPU 속도 | `DNN module was not built with CUDA backend; switching to CPU` | 이 Jetson에 설치된 OpenCV는 DNN 모듈이 CUDA 지원 없이 빌드됨 | (OpenCV DNN 경로는 포기하고) TensorRT 변환으로 우회 |
| `onnx==1.4.1` 설치 실패 | `Unknown generator option: dllexport_decl` | onnx 1.4.1과 새로 설치된 protobuf 버전이 서로 안 맞음 | `onnx==1.9.0`(README에 명시된 정확한 버전) + 최신 protobuf로 재설치 |
| `pycuda` 설치 스크립트 실패 (1) | `ERROR: nvcc not found` | CUDA 툴킷(`/usr/local/cuda/bin`)이 PATH에 없음 | `~/.bashrc`에 PATH, LD_LIBRARY_PATH 추가 |
| `pycuda` 설치 스크립트 실패 (2) | `basename: extra operand` 후 스크립트 중단 | 설치 스크립트 내부의 버전 감지 로직 버그(`set -e`라 바로 중단됨) | 스크립트 대신 `pip3 install pycuda`로 직접 설치 |
| `yolo_to_onnx.py` 실행 실패 | `ERROR: file (yolov3-tiny-416.cfg) not found!` | 변환 도구는 파일 이름에 해상도(`-416`)가 붙어있길 기대하는데, 원본 파일 이름엔 없었음 | `yolov3-tiny-416.cfg`/`.weights`로 이름 맞춰서 복사 |
| 플러그인 로드 실패 | `failed to load ../plugins/libyolo_layer.so` | `plugins` 폴더의 C++ 코드를 아직 빌드(`make`)하지 않음 | `cd plugins && make` 실행 |
| `make` 자체가 실패 | `unterminated call to function 'shell': missing ')'` | Makefile 안에 있는 정규식(`#define ...`)의 `#` 문자를, make가 "주석 시작"으로 잘못 해석해서 그 뒤 내용을 통째로 무시함 | `grep -Po` 정규식 대신 `#`이 필요 없는 `awk` 방식으로 해당 줄을 수정 |
| `onnx_to_tensorrt.py` 실행 실패 | `'tensorrt.tensorrt.Builder' object has no attribute 'build_serialized_network'` | 이 함수는 TensorRT 8 이상 API인데, 설치된 TensorRT는 7.1.3 버전이라 없음 | 코드를 TensorRT 7 방식인 `builder.build_engine(network, config)`로 수정 |
| `trt_yolo.py` 실행 실패 | `failed to load ./plugins/libyolo_layer.so` | 상대 경로(`./plugins`) 문제로, 엉뚱한 위치(홈 디렉터리)에서 실행함 | `tensorrt_demos` 폴더 안으로 `cd`한 뒤 실행 |
| GUI 창을 못 띄움 | `trt_yolo.py`가 화면(cv2.imshow)을 띄우려 함 | SSH 터미널 접속이라 디스플레이(화면 출력 장치)가 없음 | 화면 없이 콘솔에 결과와 FPS만 출력하는 스크립트(`headless_test.py`)를 따로 작성 |
| 실제 차량 반응속도가 너무 느림 | 판단(콘솔 로그)은 빠른데 실제 조향/정지가 굼뜸 | 매 프레임(초당 30번)마다 같은 조향/속도값을 계속 다시 명령해서, `Pilot.AutoCar`의 점진적 가속/조향(ramping) 로직이 계속 리셋되는 것으로 추정 | 1차: 상태가 바뀔 때만 명령 전송하도록 수정 → 그래도 동일 → 2차: "정지 후 생각"(멈춰서 방향 판단 후 짧게 이동) 방식으로 설계 자체를 변경 (`jetson_obstacle_avoidance.py`) |

**패턴으로 보면:** Jetson 쪽 문제들은 대부분 "**2020년에 나온 구버전 소프트웨어(JetPack 4.3)** vs **최근 만들어진 변환 도구/라이브러리**" 사이의 버전 불일치에서 비롯됐습니다. 앞으로도 새 라이브러리를 설치할 때는 항상 "이 Jetson이 몇 년도 소프트웨어인지"를 염두에 두고, 버전을 낮춰서 맞추는 방식으로 접근하면 됩니다.

---

## 6-1. 날짜별 작업 기록 (요약, 자세한 내용은 위 섹션 참고)

**2026-07-13**
- WSL 연습 환경(YOLOv8) 세팅, 오토카(Jetson) YOLOv3-tiny 실시간 탐지 + TensorRT 변환 (FPS 6.0→30.1)
- 장애물 회피("정지 후 생각" 방식), TTS 음성 알림, 라인 트레이싱 코드 작성 (2-9~2-11)
- 트랙 현장 라인 색상 보정 시도했으나 역광으로 미완료 (2-10)
- 라인트레이싱 대신 YOLO 사람 따라가기로 방향 전환, 코드 작성

**2026-07-14**
- 사람 따라가기 웹 디버깅 화면(`jetson_follow_person_web.py`) 실기 테스트
- **차가 안 움직이던 원인 발견/수정**: `Car.setSpeed()`가 정지 상태에선 무시되는 함수였음 → `Car.forward(speed)`로 속도 직접 전달하도록 수정
- **인식 떨림으로 정지↔전진 반복되던 문제 수정**: EMA 스무딩 + 미스 유예(6프레임) + 명령 최소 유지시간(0.4초) 적용
- 스레드 구조 정리: YOLO/제어(메인) / 화면 그리기·인코딩(별도) / Flask 서버(별도) 3단 분리
- 거리 판정 기준값 조정 (`0.05/0.1/0.15` → `0.08/0.2/0.35`, 실내 거리에 맞게 완화)
- 카메라가 천장을 보고 있는 것 재발견 (라인트레이싱 때 각도 그대로 남아있었음, 조정 필요)
- SSH 직접 접속 환경 구축(비밀번호 없이 명령 실행), 실행 방식을 `nohup`에서 tmux(`autocar` 세션)로 전환
- 팀 GitHub 저장소 분석 (전체 시스템 구조: 라이다+AI카메라+폰조종+tmux 4분할, 포트 5000 팀원 코드와 겹침 확인), 본인 예전 라이다 코드(`lidar_follow.py`) 재발견
- 안 쓰는 파일 정리 (WSL/Jetson 양쪽)
- **팀 저장소의 폰 조종 코드(`app.py`+`remote.html`) 가져와서 통합**: `phone_remote_control.py` 하나로 MANUAL(폰 조종)/AUTO(YOLO 사람 추적) 모드 다 처리하도록 재작성. `jetson_follow_person_web.py`(영상 스트리밍 버전)는 이걸로 완전히 대체되어 삭제
- **라이다를 항상 최우선 안전장치로 통합**: MANUAL/AUTO 모드 상관없이 좌우 700mm 이내 장애물 감지되면 무조건 회피부터 실행하도록 구현
- **시동/종료 버튼**: 항상 켜져있는 가벼운 `launcher.py`(포트 5001)를 별도로 만들어서, 여기서 tmux로 무거운 `phone_remote_control.py`를 켜고 끔 (폰에서 버튼으로 시동/종료 가능)
- **사람 놓치면 카메라로 탐색**: `Car.camPan()`으로 카메라 서보를 90(중앙)→0→180→90 순서로 아주 천천히 왕복(1렙), 20렙 반복해도 못 찾으면 AUTO 모드 자동 종료. 다시 찾으면 그 자리에서 멈추고 바로 추적 재개
  - 처음엔 인식이 깜빡거릴 때마다 중앙으로 순간 점프해서 "확 도는" 것처럼 보이는 버그가 있었음 → 중앙 복귀도 다른 구간과 동일하게 한 스텝씩 이동하도록 수정
- **원인불명 크래시/멈춤 문제 해결**:
  1. 라이다를 별도 스레드로 돌리면 카메라/YOLO와 동시 실행되면서 프로세스가 통째로 죽음 → 스레드 없애고 메인 루프에서 순서대로 처리(폴링)하도록 변경
  2. 그래도 라이다 연결 시점에 가끔 죽거나 멈춤 (카메라/GPU가 이미 전력·자원을 쓰는 상태에서 라이다가 끼어들면서 생기는 것으로 추정) → 연결 전 3초 대기 + **라이다 연결에 10초 제한시간을 걸어서, 안 되면 라이다 없이라도 폰 조종/YOLO는 정상 작동**하도록 안전장치 추가
- **네트워크가 하루 종일 불안정**: USB(`192.168.55.1`)와 와이파이를 오가며 작업했는데 둘 다 수시로 끊김 (와이파이는 IP도 매번 바뀜). USB는 로컬 전용이라 인터넷(git clone 등)은 안 되고, 인터넷 필요한 작업은 와이파이가 필수라는 것 확인
- **GitHub 팀 저장소 정리**: 로컬에서 지운 파일들이 저장소엔 남아있던 것 발견 → `team` 리모트로 정리 커밋(`team-cleanup` 브랜치) 만들어놨으나, 이 환경에 GitHub push 인증(토큰)이 없어서 직접 못 올림 → 본인이 `git push team team-cleanup:main` 실행하거나 기존 동기화 방식에 맡기기로 함
- **다른 차량("vacationing") 발견**: 같은 소프트웨어 버전(Python 3.6.9, TensorRT 7.1.3)이지만 `pop`은 시스템에 설치돼 있고 `tensorrt_demos`가 아예 없어서 처음부터 세팅 필요 → git clone 중 네트워크가 끊겨서 중단, **본래 쓰던 차(`february`) 충전되면 그걸로 계속하기로 하고 새 차량 세팅은 보류**

**2026-07-15**
- **호스트네임 미스터리 해결**: `february`/`vacationing`/`halifax` 등 매번 다른 이름으로 보였던 게 사실 전부 **같은 차량 1대**였음 — 재부팅될 때마다 호스트네임이 랜덤 단어로 바뀌는 방식이었던 것. `tensorrt_demos` 폴더 존재 여부로 같은 기기인지 확인 가능
- **차가 명령 없이 혼자 움직이는 심각한 버그 발견/수정**: 라이다가 근접 장애물을 감지하면 **MANUAL 모드에서도 매 프레임 무조건 회피 명령을 반복 전송**하는 구조였음 → 노이즈성 오탐 한두 번에도 차가 갑자기 움직임 → **연속 5번 이상 감지돼야 "진짜 장애물"로 인정**하는 디바운스 추가로 해결
- **카메라 탐색이 오른쪽 끝까지 못 가던 버그 수정**: 사람 인식이 깜빡거릴 때마다 탐색 진행 상황(90→0→180→90 순서 어디까지 갔는지)이 처음부터 리셋되고 있었음 → **연속 3프레임 이상 사람이 확인돼야 "진짜로 찾음"으로 인정**하도록 수정, 그 전까진 탐색 진행 상황 유지
- **카메라 각도 계산 근본 오류 발견/수정**: 공식 API 문서 확인 결과 `camPan(value)`는 **0~180 절대각도**를 그대로 받는 함수인데, 코드에서 `90 - 각도`로 변환해서 엉뚱한 값을 보내고 있었음 → 변환 제거하고 절대각도 그대로 전달하도록 수정 (이게 탐색이 이상하게 움직이던 진짜 근본 원인)
- **라이다 연결 시 완전 멈춤(GIL 교착) 문제 최종 해결**: 스레드+타임아웃으로도 못 막을 만큼, 라이다 연결이 멈추면 GIL을 붙잡아서 폰 조종까지 전부 먹통이 되는 문제였음 → **라이다를 완전히 별도 OS 프로세스(`lidar_reader.py`)로 분리**, 메인 프로그램은 그 프로세스가 써놓는 상태 파일(`/tmp/lidar_state.json`)만 읽음. 이제 라이다가 죽어도 메인 프로그램(폰 조종+YOLO)은 전혀 영향 없음
- 네트워크 문제로 여러 차례 재연결 (USB/와이파이 번갈아 사용, IP 여러 번 재확인)
- **파일 정리**: `phone_remote_control.py`, `launcher.py`, `lidar_reader.py`, `templates/`를 `사람따라가기_프로젝트/` 하위 폴더로 이동해서 정리
- **차가 명령 없이 벽으로 돌진한 안전 문제 해결**: 라이다 감지 거리(700mm)가 정지 반응 시간에 비해 너무 짧아서 멈추기 전에 충돌 → **감지 거리를 1300mm로 확대**
- **라인트레이싱을 폰 웹뷰 세 번째 모드로 통합**: 카메라가 하나뿐이라 YOLO(AUTO)와 라인트레이싱(LINE)을 별도 프로세스로 동시에 못 띄운다는 걸 확인 → `phone_remote_control.py` 하나 안에 MANUAL/AUTO/LINE 모드로 통합, 폰 화면에 모드 버튼 4개(수동 라이다O/X, 사람추적, 라인트레이싱)로 재구성
  - 라인트레이싱 로직은 예전 `line_calibration.py`의 채도 기반 방식(역광에 더 안정적이었던 방식)을 실제 주행 코드로 연결
  - 카메라 노출을 `exposurecompensation=-1.5`로 낮춰서 햇빛에 화면 날아가는 문제 완화 시도 (커스텀 GStreamer 파이프라인 직접 작성, `pop.Util.gstrmer()` 대신 사용)
  - **라인트레이싱 속도 버그**: `LINE_FORWARD_SPEED=35`였는데 이 모터는 50 미만이면 실제로 안 굴러감(경험적으로 이미 확인된 값인데 빠뜨림) → 60으로 수정
  - **라인 인식 ROI 범위 오류**: 화면 아래 48%만 분석하도록 되어있었는데, 실제 카메라 각도에서는 흰 선이 그보다 위쪽(화면 상단 쪽)에 걸쳐 있어서 아예 분석 범위 밖이었음 → ROI를 아래 75%(`LINE_ROI_TOP_RATIO=0.25`)로 넓혀서 해결. **디버그 화면(`/debug/line.jpg`)으로 실제 카메라 시야를 직접 캡처해서 확인**했기 때문에 발견 가능했던 문제
  - LINE 모드 전환 시 카메라를 자동으로 수평 90도(정중앙)로 맞추도록 추가 (`/api/mode`에서 처리)
- **디버그 화면 추가**: `/debug/line.jpg` - 계속 스트리밍하지 않고 새로고침할 때마다 최신 카메라 화면 + 인식된 선(초록색 오버레이) + 중앙 기준선 확인 가능 (과거 line_calibration.py의 사진 찍기 방식을 실시간 웹으로 대체)
- **카메라 상하 각도(tilt) 수동 조작 버튼 추가**: 자동 동작 없이 폰에서 위/아래 버튼으로만 (`Car.camTilt()`, -30~200 범위)
- **수동 모드 2종 분리**: "수동(라이다 O)"과 "수동(라이다 X)" — 벽에 가까이 붙여서 의도적으로 조종해야 할 때 라이다 개입을 끌 수 있게 함
- **여러 명이 동시에 폰으로 접속할 때 페이지가 옛날 버전으로 캐시되는 문제 방지**: `/remote` 응답에 `Cache-Control: no-store` 헤더 추가. (참고: 조작 명령 자체는 서버가 공유 상태 하나를 쓰는 구조라 원래부터 "마지막에 누른 사람이 우선"으로 동작함, 별도 조치 불필요)

**2026-07-15 (라인트레이싱 신뢰성 개선, 이어서)**

- 문제: LINE 모드로 실제로 돌려보니 흰 선을 아예 못 찾거나("선 없음-정지"), 엉뚱한 곳을 선으로 착각함.
- **원인 1 - 너무 어두워서 선을 못 찾음**: 흰 선인지 판단하는 기준이 "밝기가 170 이상이면 흰색"이라는 고정값이었는데, 흐린 날씨 때문에 카메라를 어둡게 찍고 있어서 흰 선도 170을 못 넘음 → **"주변 트랙보다 상대적으로 밝은가"를 보는 방식(adaptiveThreshold)으로 교체**. 날씨/노출이 바뀌어도 안정적으로 동작.
- **원인 2 - 하늘/건물을 선으로 착각**: 인식 범위(ROI)가 화면 위쪽 25%부터였는데, 그 안에 하늘/건물 윤곽선까지 포함돼서 그걸 선으로 오인식 → 인식 범위를 화면 아래 55%(트랙 바닥만)로 좁힘.
- **원인 3 - 트랙 표면 균열을 선으로 착각**: 트랙 바닥 균열도 밝기 차이가 있어서 작은 점처럼 걸림 → 일정 크기 이상으로 뭉친 덩어리만 선으로 인정하도록 필터 추가.
- **원인 4 - 점선이 끊어져서 각 조각이 너무 작음**: 트랙 페인트가 닳아서 점선처럼 끊어져 있는데, 조각 하나하나는 크기 필터에 걸려서 다 무시됨 → 끊어진 조각들을 먼저 이어붙인(dilate) 다음 필터링하도록 수정.
- **선 추적 방식 확정**: 원래는 "양쪽 레인 선 사이 중앙 유지"로 설계돼 있었는데, 실제 트랙엔 양옆 선 말고 **중앙에 점선 하나**가 있는 구조라는 걸 확인 → 좌/우로 나누는 로직을 없애고, **가장 크게(길게) 잡힌 흰 선 덩어리 하나를 추적**해서 화면 중앙과의 차이로 조향하는 단순한 방식으로 변경. 후보가 여러 개면 그중 가장 큰 것 → 그중에서도 차와 가까운 것(화면 아래쪽) 순으로 고름.
- **카메라 각도 자동 고정**: LINE 모드로 바꾸는 순간 카메라를 좌우 정중앙(pan 90°) + 아래쪽(tilt -10°)으로 자동 정렬하도록 추가 (매번 손으로 안 맞춰도 됨).
- **디버그 화면을 대시보드 안에 내장**: 예전엔 별도 링크(`/debug/line.jpg`)를 새 탭으로 열어야 했는데, 조종 화면 안에 사진 + "새로고침" 버튼을 넣어서 바로 확인 가능하게 함 (계속 스트리밍하면 느려지니까 버튼 누를 때만 갱신).
- **처리 속도 문제 발견/개선**: 차 속도가 인식 속도보다 빨라서 선을 자꾸 놓친다는 문제 제기 → (1) 선 인식 계산을 절반 해상도로 줄여서 빠르게, (2) 디버그 사진 만드는 작업(사람이 볼 때만 필요)을 매 프레임이 아니라 0.3초에 한 번만 하도록 줄임.
- **카메라 프레임이 밀리는 근본 문제 발견**: gstreamer 파이프라인의 `appsink`에 프레임을 버리는 설정이 없어서, 처리가 느려지면 오래된(과거) 프레임이 계속 쌓였다가 나오는 현상이 있었음 → `appsink drop=true max-buffers=1 sync=false` 옵션 추가해서 항상 최신 프레임만 보게 수정. (디버그 사진이 실제 차 상태와 다르게 보였던 것도 이 문제였을 가능성 높음)
- **LINE 모드에서 선을 놓치면 다시 찾는 기능 추가**: AUTO 모드가 사람을 놓쳤을 때 카메라를 좌우로 훑어서 재탐색하는 것과 똑같은 방식을, LINE 모드에서 선을 5프레임 이상 연속으로 못 찾았을 때도 하도록 추가. 찾으면 카메라를 다시 정중앙으로 돌리고 주행 재개.
- **LED 자동 제어(조도센서 연동) 시도 → 조도센서는 완료, LED는 방법을 못 찾아서 보류**
  - **조도센서 확인 완료**: `pop.Cds` 클래스(SPI ADC로 읽는 조도센서, `Cds(channel)`)가 있었음. 채널 0~7을 순서대로 읽어보고, 손으로 가려가며 실측한 결과 **채널 7이 실제 조도센서로 확정** (`Cds(7).readAverage()`로 읽으면 됨. 어두울수록 값이 커짐 — 가렸을 때 1985 근처, 안 가렸을 때 1100~1200대로 관찰됨. 정확한 밝다/어둡다 기준값은 실제 새벽/낮 환경에서 다시 재보고 정해야 함).
    - (착오 정정) 처음엔 `0x5c` I2C 주소를 조도센서(BH1750)로 착각했는데, 실제로는 카메라 팬/틸트 제어 칩이었음. 이 프로젝트의 조도센서는 I2C가 아니라 **SPI ADC 방식**(`pop.Cds`)이 맞음.
  - **LED는 못 찾음**: 사용자가 보내준 보드 레이아웃 사진(`AIoT AutoCAR Layout`)에 Left/Right Front LED, Left/Right Rear LED가 실제로 있고 "LED/Servo Motor Controller"라는 칩이 카메라 팬/틸트 바로 옆에 표시되어 있음. 즉 **LED는 카메라 팬/틸트와 같은 PWM 칩(I2C `0x5c`)의 다른 채널로 연결되어 있을 것으로 추정**했으나:
    - GPIO 핀 약 20개(23,24,25,4,5,6,7,12,13,16~22,27 등) 전부 테스트 → 반응 없음
    - `0x5c` 칩의 안 쓰는 PWM 채널(0~12번, 카메라/조향이 안 쓰는 채널) 전부 테스트 → 반응 없음
    - **결론: 지금 쓰는 `pop` 파이썬 라이브러리(이 차종 `_cat=6`, AutoCar Prime+NX 기준)엔 LED를 켜고 끄는 명령어 자체가 없음.** 보드에 LED가 물리적으로 있는 건 확실하지만, 소프트웨어로 제어하는 방법은 라이브러리 문서/제조사 쪽에서 별도로 찾아야 함 (남은 미시도 핀: I2C/SPI/UART 전용으로 예약된 0,1,2,3,8,9,10,11,14,15번은 지금 조도센서/카메라/라이다가 실제 쓰고 있어서 함부로 테스트 안 함 — 잘못 건드리면 그 기능들이 깨질 수 있음).
  - **(정정, 이후 해결됨)** 위 결론은 틀렸음. 사용자가 갖고 있던 예제 코드(`Pilot.PWM(1, 0x5c)` 직접 사용) 덕분에, LED도 카메라 팬/틸트와 **같은 PWM 칩(0x5c)의 다른 채널**로 제어된다는 게 확인됨. `pop.Pilot.AutoCar` 객체에 래핑된 편의 메서드가 없을 뿐, `Pilot.PWM` 클래스로 직접 접근하면 됨.
  - 채널 하나씩 켜보고 실물로 확인해서 매핑 확정: **채널 0=오른쪽 앞, 1=왼쪽 앞, 2=오른쪽 뒤, 3=왼쪽 뒤** (4번부터는 반응 없음, freq는 50Hz, duty=99가 켬/0이 끔).
  - 조도센서는 `pop.Cds(7)`로 확정 (SPI ADC 채널 7). 실측 결과 값이 작을수록 어두운 쪽으로 확인해서 방향 확정.
  - `phone_remote_control.py`에 반영: `/api/led?mode=AUTO|ON|OFF` — AUTO면 조도센서 보고 자동으로 켜고 끔(2초마다 체크), ON/OFF면 수동 고정. 폰 화면에 버튼 3개 추가.

**2026-07-15 (라인트레이싱 심화 - 장애물 복귀/사람 추적 유지, 시행착오 포함)**

- **AUTO 모드 - 사람 여러 명 있을 때 다른 사람으로 갈아타는 문제 수정**: `get_person_box()`가 항상 "제일 큰(가까운) 사람"만 고르던 걸, 직전에 추적하던 사람 위치(`last_center`)와 가장 가까운 사람을 우선 고르도록 변경. 처음 찾을 때(추적 이력 없음)만 예외적으로 제일 큰 사람을 고름.
- **라이다 프로세스가 멈춰서(행) 장애물을 전혀 못 피하던 버그 발견/수정**: `/tmp/lidar_state.json`의 `connected`가 계속 `false`였고 타임스탬프도 몇 분째 그대로였음 → `lidar_reader.py` 프로세스가 `connect()` 안에서 응답 없이 완전히 멈춰있었던 것(별도 프로세스로 분리해놨어도, 그 프로세스 자체가 멈추는 경우가 실제로 있었음). **해결**: `lidar_reader.py` 안에 감시자(부모) - 실제 작업(자식, `multiprocessing.Process`) 구조를 한 겹 더 둬서, 자식이 `CONNECT_TIMEOUT_SEC(10초)` 안에 연결 못 하거나 멈추면 부모가 강제로 죽이고(`kill`) 새 자식으로 재시도하도록 함. 이러면 내부에서 완전히 멈춰도 바깥에서 살릴 수 있음.
- **LINE 모드 - 장애물 피한 뒤 원래 선으로 복귀하는 기능 추가/시행착오**:
  - 처음엔 "직전 선 위치(`last_x`)와 가장 가까운 덩어리"를 **크기 상관없이** 무조건 채택하도록 바꿨는데, 실기 테스트에서 두 가지 부작용 발견: (1) 진짜 선이 하나도 없는 장면(배수구 철망 등)에서 작은 노이즈에 억지로 고정됨, (2) 진짜 선은 화면 반대쪽에 있는데 위치만 가깝다는 이유로 작은 엉뚱한 조각에 눌러붙어서 조향이 한쪽으로 고정(steer=1.00)되는 문제.
  - **최종적으로는 되돌림**: "일단 크게 잡힌 덩어리들(최대 크기의 70% 이상)" 안에서만 고르고, 그중에서 직전 위치와 가까운 걸 우선하는 기존(오전에 검증됐던) 방식을 유지하기로 함. 크기 비교를 완전히 없애는 건 오히려 더 나쁜 실패를 만든다는 걸 실기로 확인함.
  - **추가로 확인된 근본적 한계**: 트랙에 그려진 원형 로고/마크처럼 선이 아닌 다른 큰 흰색 무늬가 있으면, "제일 큰 흰 덩어리 = 선" 방식이 그쪽으로 잘못 갈 수 있음. 이건 오늘 튜닝으로 완전히 해결하지 못했고, 다음에 "가늘고 길쭉한 모양만 선으로 인정"하는 형태(종횡비) 기반 필터를 추가하면 개선될 것으로 보임.
  - 최근 30초간 선의 좌우 치우침 추세(`line_error_history`)를 기록해뒀다가, 선을 놓쳐서 재탐색할 때 그 추세 방향부터 먼저 훑도록 하는 기능은 유지함 (반대 방향으로 헛도는 것 방지).
- **LINE 모드 장애물 대응 방식 변경**: 다른 모드(MANUAL/AUTO)는 기존처럼 후진+회전으로 회피하지만, **LINE 모드는 후진/회전을 하면 트랙에서 완전히 벗어나 엉뚱한 무늬로 갈아탈 위험이 커서, 장애물 감지되면 그냥 멈췄다가 장애물이 사라지면 다시 출발**하도록 변경함.

**남은 과제**: 트랙에 로고 등 선이 아닌 다른 흰 무늬가 있는 구간에서 오작동하는 근본 문제는 아직 미해결. 다음 세션에서 종횡비(가늘고 긴 모양) 기반 필터를 추가해보는 게 다음 시도로 좋을 것 같음.

---

## 6. 다음에 할 일 (TODO)

- [x] ~~사람 따라가기 코드 작성 + 실기 디버깅~~ → 속도/떨림 버그 수정 완료
- [x] ~~라이다 실제 연결 및 테스트~~ → `phone_remote_control.py`에 안전 회피로 통합, 연결 확인됨
- [x] ~~폰 조종 기능~~ → 팀 저장소 코드 가져와서 통합, 시동/종료 버튼까지 완료
- [x] ~~사람 놓쳤을 때 카메라 탐색 기능~~ → 완료
- [x] ~~카메라 탐색 시 중앙으로 확 튀던 버그~~ → 완료
- [x] ~~라이다 연결이 가끔 멈추던 문제~~ → 완전 별도 프로세스로 분리해서 근본 해결
- [x] ~~차가 명령 없이 혼자 움직이던 안전 버그~~ → 디바운스 추가로 완료
- [x] ~~카메라 탐색이 오른쪽 끝까지 못 가던 버그~~ → 완료
- [x] ~~카메라 절대각도 계산 오류(camPan)~~ → 완료
- [x] ~~라이다 실제 회피 동작 확인~~ → AVOIDING (RIGHT)/(BACK) 등 실제 동작 확인 완료
- [x] ~~라이다 감지 거리가 짧아 벽 충돌하던 문제~~ → 700mm→1300mm로 완료
- [x] ~~라인트레이싱을 폰 웹뷰 모드로 통합~~ → MANUAL/AUTO/LINE 하나의 프로그램으로 완료
- [x] ~~라인트레이싱 속도/ROI 범위 버그~~ → 완료 (디버그 화면으로 원인 확인)
- [x] ~~라인 인식 디버그 화면~~ → `/debug/line.jpg` 완료
- [x] ~~카메라 상하 각도 수동 조작~~ → 완료
- [x] ~~라이다 켜고 끄는 수동 모드 분리~~ → 완료
- [x] ~~라인트레이싱 인식 신뢰성 개선~~ → 상대 밝기 방식, ROI 축소, 노이즈 필터, 점선 이어붙이기, 중앙선 단일 추적 방식으로 변경 완료
- [x] ~~라인트레이싱 처리 속도 개선~~ → 절반 해상도 처리 + 디버그 화면 생성 빈도 제한 + 카메라 프레임 밀림(appsink drop) 수정 완료
- [x] ~~LINE 모드 선 놓치면 재탐색~~ → AUTO와 같은 방식으로 완료
- [ ] 실제 트랙에서 라인트레이싱 장시간 주행 테스트 (오늘 수정 반영 후 재확인 필요)
- [ ] 다른 사람 폰에서 웹뷰 접속 안 되는 문제 확인 필요 (같은 네트워크/IP인지부터 점검)
- [x] ~~조도센서 채널 확인~~ → `pop.Cds(7)`이 실제 조도센서로 확정 (SPI ADC 채널 7)
- [x] ~~LED 소프트웨어 제어~~ → `Pilot.PWM(1, 0x5c)` 채널 0~3으로 확정, 자동(조도센서)/수동 켜기·끄기 버튼까지 완료
- [x] ~~AUTO 모드 사람 여러 명 있을 때 다른 사람으로 갈아타는 문제~~ → 직전 추적 위치와 가까운 사람 우선하도록 완료
- [x] ~~라이다 프로세스가 연결 도중 멈춰서(행) 장애물을 전혀 못 피하던 버그~~ → 감시자(부모)-작업자(자식) 구조로 타임아웃 시 강제 재시작하도록 완료
- [x] ~~LINE 모드 장애물 회피 방식~~ → 후진/회전 대신 그냥 멈췄다 재출발하도록 변경 완료 (엉뚱한 무늬로 갈아타는 위험 줄임)
- [ ] **[미해결, 다음 우선순위] LINE 모드가 트랙 위 로고/원형 마크 등 선 아닌 다른 흰 무늬를 선으로 오인하는 문제**: "제일 큰 흰 덩어리 = 선"으로 판단하는 지금 방식의 근본 한계. 다음엔 종횡비(가늘고 긴 모양)로 필터링하는 방식을 시도해볼 것. 오늘 "직전 위치 우선(크기 무시)"으로 바꿔봤다가 더 나쁜 부작용(진짜 선 없는 곳에서 노이즈에 고정)만 확인하고 기존 방식으로 되돌림 — 급하게 재시도하지 말고 시간 들여서 다시 볼 것.
- [ ] 실제 트랙에서 라인트레이싱 장시간 주행 테스트 (반복 수정 많았으니 처음부터 차근차근 재확인 필요)
- [ ] 다른 사람 폰에서 웹뷰 접속 안 되는 문제 확인 필요 (같은 네트워크/IP인지부터 점검)
- [ ] 폰으로 MANUAL/AUTO 실기 조종 테스트 계속 진행 (안정화된 지금부터 본격적으로)
- [ ] GitHub 팀 저장소 정리 커밋 반영 (`git push team team-cleanup:main`, 본인이 직접 실행 필요)
- [ ] TrtYOLO 초기화 시간이 길어서(15초+) 시동 버튼 누른 뒤 대기 시간 있음 - 데모 때 감안 필요
- [ ] Jetson 재부팅이 세션 중 여러 번 발생함(호스트네임 halifax→conflagrations→monopolizes로 바뀜) - 원인 불명, 전원/와이파이 불안정 쪽으로 의심됨. daemon 등록해두면 재부팅돼도 자동 복구는 되지만 원인 자체는 따로 확인 필요
- [ ] daemon(systemctl) 등록해서 전원 켜면 런처가 자동 실행되게 설정

---

*이 문서는 진행 상황에 따라 계속 업데이트됩니다.*
